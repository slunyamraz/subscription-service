name: Go CI - First Step

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "🎉 Мой первый CI/CD для Go проекта!"

      - name: Check Project Structure
        run: |
          echo "Смотрю что в проекте:"
          ls -la
          echo "---"
          echo "Go модуль:"
          cat go.mod